{
  "title": "LaunchModel \u2014 API Overview",
  "timezone": "browser",
  "panels": [
    {
      "type": "graph",
      "title": "Requests",
      "targets": [
        {
          "expr": "sum(rate(inference_requests_total[1m]))"
        }
      ]
    },
    {
      "type": "graph",
      "title": "p50/p95 Latency",
      "targets": [
        {
          "expr": "histogram_quantile(0.5, sum(rate(inference_latency_seconds_bucket[5m])) by (le))"
        },
        {
          "expr": "histogram_quantile(0.95, sum(rate(inference_latency_seconds_bucket[5m])) by (le))"
        }
      ]
    }
  ]
}